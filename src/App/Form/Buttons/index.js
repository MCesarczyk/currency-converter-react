import { ButtonsContainer, Button } from "./styled";

const Buttons = () => (
    <ButtonsContainer>
        <Button>
            Przelicz
        </Button>
        <Button type="reset">
            Wyczyść
        </Button>
    </ButtonsContainer>
);

export default Buttons;